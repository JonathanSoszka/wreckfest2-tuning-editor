<Window x:Class="Wf2App.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Wf2App"
        Title="Settings" SizeToContent="Height" Width="460" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource Bg}" Foreground="{StaticResource Ink}"
        FontFamily="{DynamicResource AppFontFamily}" FontSize="{DynamicResource AppFontSize}"
        UseLayoutRounding="True" TextOptions.TextFormattingMode="Display">
    <StackPanel Margin="18">
        <TextBlock Text="APPEARANCE" FontSize="11" FontWeight="Bold"
                   Foreground="{StaticResource Muted}" Margin="0,0,0,12" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Font" VerticalAlignment="Center" />
            <ComboBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="90,0,0,0"
                      ItemsSource="{Binding Fonts}" SelectedItem="{Binding SelectedFont}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Source}" FontFamily="{Binding}" FontSize="14" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Size" VerticalAlignment="Center" Margin="0,12,0,0" />
            <ComboBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="90,12,0,0" Width="90"
                      HorizontalAlignment="Left"
                      ItemsSource="{Binding Sizes}" SelectedItem="{Binding FontSize}" />
        </Grid>

        <!-- Live preview -->
        <Border Margin="0,18,0,0" Padding="12" CornerRadius="6"
                Background="{StaticResource Tree}" BorderBrush="{StaticResource Line}" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Preview" FontSize="10" Foreground="{StaticResource Muted}" Margin="0,0,0,6" />
                <TextBlock Text="{Binding PreviewText}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="Cancel" IsCancel="True" Padding="14,4" MinWidth="80" />
            <Button Content="Save" Click="Save_Click" Style="{StaticResource AccentButton}"
                    Padding="14,4" MinWidth="90" Margin="8,0,0,0" />
        </StackPanel>
    </StackPanel>
</Window>
